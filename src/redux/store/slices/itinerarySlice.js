import { createSlice } from '@reduxjs/toolkit';
export const itinerarySlice = createSlice({
   name: 'itinerary',
   initialState: {
    
   },
    reducers: {
     
    }
});
export const { increment } = itinerarySlice.actions;